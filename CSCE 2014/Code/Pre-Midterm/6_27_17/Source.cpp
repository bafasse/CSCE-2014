


//{1, 4, 7, 8, 19, 24, 25, 84}

//Linear Search:
//array does not have to be sorted

//for (int i = 0; i < size; ++i)
//	if (data[i] == c)
//		return i;
//return -1;

//Binary Search

//(1) find midpoint
//(2) check midpoint if (data(mid) == value)
//(3) if found, return mid
//(4) if not found, search left or right half og the array
//runtime = O(log(base 2)(N))